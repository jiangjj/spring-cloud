eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka/
security:
  oauth2:
    resource:
      user-info-uri: http://api-gateway:9000/oauth2/user
signing:
  key: thisissigningkey